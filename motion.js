const reduceMotion=window.matchMedia('(prefers-reduced-motion: reduce)');
const motionToggle=document.getElementById('motion-toggle');
function setMotion(off){document.body.classList.toggle('motion-off',off);motionToggle.setAttribute('aria-pressed',String(!off));motionToggle.innerHTML='Motion '+(off?'off':'on')+' <span>'+ (off?'○':'◉')+'</span>';if(off)document.querySelectorAll('.will-reveal').forEach(el=>el.classList.add('revealed'))}
setMotion(reduceMotion.matches);motionToggle.addEventListener('click',()=>setMotion(!document.body.classList.contains('motion-off')));reduceMotion.addEventListener('change',e=>setMotion(e.matches));
const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('revealed');revealObserver.unobserve(entry.target)}}),{threshold:.09});
function prepareMotion(){if(reduceMotion.matches||document.body.classList.contains('motion-off'))return;document.querySelectorAll('.exhibit-main,.exhibit-side,.creative-strip,.work-card,.brief,.funding-card,.perk,.feature-group').forEach((el,i)=>{if(el.classList.contains('will-reveal'))return;el.style.setProperty('--reveal-delay',Math.min(i%3*75,150)+'ms');el.classList.add('will-reveal');revealObserver.observe(el)})}
const appObserver=new MutationObserver(prepareMotion);appObserver.observe(document.getElementById('app'),{childList:true});prepareMotion();
document.addEventListener('pointermove',e=>{if(reduceMotion.matches||document.body.classList.contains('motion-off')||e.pointerType==='touch')return;const target=e.target.closest('.exhibit-image');if(!target)return;const r=target.getBoundingClientRect();target.style.setProperty('--pointer-x',((e.clientX-r.left)/r.width-.5)*8+'px');target.style.setProperty('--pointer-y',((e.clientY-r.top)/r.height-.5)*6+'px')},{passive:true});
